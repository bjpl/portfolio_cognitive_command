# Portfolio Cognitive Command v8.0 - Environment Configuration

# ============================================
# OpenAI API Configuration
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Configure OpenAI model (default: text-embedding-3-small)
# Available models: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Optional: Configure embedding dimensions (default: 1536)
# Valid values: 256, 512, 1024, 1536 (standard), 3072 (for large model)
# EMBEDDING_DIMENSIONS=1536

# Optional: Disable OpenAI API (force fallback mode)
# Set to false to use keyword-based embeddings only
# USE_OPENAI_API=true

# ============================================
# Project Paths
# ============================================
# Directory to scan for projects (default: current working directory)
# SCAN_DIR=/path/to/your/projects

# Path to agent database JSON file (default: ./data/agentdb.json)
# AGENTDB_PATH=./data/agentdb.json

# Output directory for analysis results (default: ./output)
# OUTPUT_DIR=./output

# ============================================
# Analysis Settings
# ============================================
# Drift detection threshold (0-1, default: 0.7)
# Higher = more sensitive to drift, Lower = less sensitive
# DRIFT_THRESHOLD=0.7

# Minimum cluster size for grouping (default: 2)
# MIN_CLUSTER_SIZE=2

# Maximum cluster size for grouping (default: 10)
# MAX_CLUSTER_SIZE=10

# ============================================
# System Settings
# ============================================
# Enable verbose logging (default: false)
# VERBOSE=false

# Log level: error, warn, info, debug (default: info)
# LOG_LEVEL=info

# Fallback to keyword-based embeddings if API fails (default: true)
# FALLBACK_TO_KEYWORDS=true

# ============================================
# Notes
# ============================================
# - If OPENAI_API_KEY is not set, the system automatically uses
#   keyword-based embedding fallback with no API calls
# - All paths can be absolute or relative to the project root
# - Boolean values should be 'true' or 'false' (lowercase)
# - Restart the application after changing configuration
