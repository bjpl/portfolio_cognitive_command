# Portfolio Cognitive Command v8.0 - Environment Configuration

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Configure OpenAI model (default: text-embedding-3-small)
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Optional: Configure embedding dimensions (default: 1536)
# EMBEDDING_DIMENSIONS=1536

# Optional: Disable OpenAI API (force fallback mode)
# USE_OPENAI_API=false

# Note: If OPENAI_API_KEY is not set, the system will automatically
# use keyword-based embedding fallback with no API calls
